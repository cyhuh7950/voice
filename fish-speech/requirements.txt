# fish-speech (S2, v2.0.0-beta) 의존성 — x86_64 AMD ROCm (WSL/ROCDXG)
#
# ★ torch / torchaudio 를 여기 적지 말 것 ★
#   업스트림 pyproject.toml 은 torch==2.8.0 / torchaudio==2.8.0 을 핵심 의존성에 못박는다.
#   그대로 설치하면 PyPI 의 CUDA 빌드가 베이스 이미지의 ROCm 빌드를 덮어써서 GPU 를 잃는다.
#   그래서 `pip install fish-speech` 대신 아래 목록을 직접 설치한다.
#   fastapi/uvicorn/python-multipart/numpy/soundfile 는 베이스에 이미 있다.
#
# 목록은 업스트림 pyproject.toml 의 dependencies 에서 torch/torchaudio 만 뺀 것이다.
# 학습·웹데모용으로 보이는 것(datasets/lightning/tensorboard/gradio/wandb 등)도 남겨뒀는데,
# CosyVoice3 이식 때 "학습 전용"으로 판단해 뺀 rich/gdown/matplotlib/wget 이 실제로는
# 추론 경로의 동적 import 체인에 걸려 기동이 네 번 깨졌기 때문이다. 검증 후 줄일 것.

transformers<=4.57.3
datasets==2.18.0
lightning>=2.1.0
hydra-core>=1.3.2
tensorboard>=2.14.1
natsort>=8.4.0
einops>=0.7.0
librosa>=0.10.1
rich>=13.5.3
gradio>5.0.0
wandb>=0.15.11
grpcio>=1.58.0
kui>=1.6.0
loguru>=0.6.0
loralib>=0.1.2
pyrootutils>=1.0.4
resampy>=0.4.3
einx[torch]==0.2.2
zstandard>=0.22.0
pydub
pyaudio
modelscope==1.17.1
opencc-python-reimplemented==0.1.7
silero-vad
ormsgpack
tiktoken>=0.8.0
pydantic==2.9.2
cachetools
descript-audio-codec
descript-audiotools
safetensors
huggingface-hub
