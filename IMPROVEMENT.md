# 개선 전략 — 소스 수정과 자체 학습

## 이 프로젝트의 목적

STT/TTS 엔진 8종을 설치한 것은 **그대로 갖다 쓰기 위해서가 아니다.**
소스를 수정하고 자체 데이터로 학습시켜 품질을 끌어올리는 것이 목적이다.

따라서 판단 기준이 일반적인 "설치·운영"과 다르다.

- 엔진 선택은 성능뿐 아니라 **수정 가능성과 학습 가능성**을 함께 본다
- 업스트림 저장소는 통째로 들고 있는다 (`<엔진>/repo/`). pip 설치만으로 끝내지 않는다
- 우회(workaround)를 넣을 때는 **왜 그렇게 했는지**를 반드시 주석으로 남긴다.
  나중에 소스를 고쳐 정공법으로 풀 때 그 주석이 출발점이 된다

---

## 1. 엔진별로 수정할 수 있는 층

같은 "오픈소스"라도 수정 난이도가 다르다. 세 층으로 나뉜다.

| 층 | 엔진 | 수정 방법 | 난이도 |
|---|---|---|---|
| **저장소 벤더링** | cosyvoice(135개 .py)<br>fish-speech(59개)<br>moss-tts-nano(21개) | `<엔진>/repo/` 를 직접 고친다. git 이력도 함께 갖고 있다 | 낮음 |
| **pip 패키지 (순수 파이썬)** | whisper-gpu(transformers)<br>xtts(coqui-tts)<br>qwen3-tts(qwen-tts) | 아래 "수정 계층 선택" 참고 | 중간 |
| **컴파일된 확장** | zipformer(sherpa-onnx) | C++ 휠이라 소스 수정은 CMake 빌드가 필요하다 | 높음 |

---

## 2. 수정 계층 선택 — 위에서부터 시도한다

같은 결과를 내더라도 어느 층에서 고치느냐에 따라 유지보수 비용이 크게 달라진다.

### ① 우리 `server.py` 에서 푼다 (최우선)

업스트림을 건드리지 않고 우리 코드에서 끝나는 경우다. 가장 싸고 안전하다.
**업스트림 문제처럼 보이는 것 중 상당수가 실제로는 이 층에서 풀린다.**

실제 사례 — zipformer 띄어쓰기:

```
증상    : "안녕하세요컨테이너음성학생테스트입니다."  (공백이 전혀 없음)
첫 판단 : BPE 가 띄어쓰기 없는 전사문으로 학습됐다 → 재학습 또는 후처리기 필요
실제    : tokens.txt 에 SentencePiece 단어경계 표식(▁)이 2352개 있고,
          sherpa-onnx 가 그걸 토큰 앞 공백으로 바꿔 넘겨준다.
          tokens = [' 안','녕','하','세요',' ','컨',...,' 학생',' 테','스트',...]
          띄어쓰기는 이미 예측돼 있고 result.text 로 합치는 과정에서만 사라진다
해결    : server.py 에서 result.text 대신 "".join(tokens) 를 쓴다 (한 줄)
```

재학습도 후처리기도 필요 없었다. **증상만 보고 학습 문제로 단정하지 말 것.**

### ② 런타임 몽키패치 (pip 패키지 대상)

pip 패키지의 동작을 바꿔야 하는데 포크까지는 과할 때. 우리 `server.py` 안에 두므로
저장소에서 grep 으로 찾을 수 있고, pip 업그레이드에도 살아남는다.

실제 사례 — `torchaudio.load` → soundfile 대체 (**엔진 3곳에 동일 적용**):

```
원인 : torchaudio 2.11 부터 레거시 오디오 백엔드가 빠지고 torchcodec 에 위임된다.
       그런데 PyPI 의 torchcodec 프리빌트 라이브러리는 CUDA 빌드라
       ROCm 에서 libnvrtc.so.13 을 못 찾아 로드에 실패한다.
적용 : cosyvoice(_patch_load_wav) / moss-tts-nano / xtts 세 곳
정공법: torchcodec 을 ROCm 으로 직접 빌드하거나, 각 업스트림에 soundfile 폴백 PR
```

### ③ 빌드 시점 패치 (`.patch` 파일)

변경이 커서 몽키패치로 표현하기 어려울 때. `<엔진>/patches/*.patch` 를 두고
Dockerfile 이 `pip install` 다음에 적용한다. 아직 쓰는 곳은 없다.

### ④ 포크 후 git 설치

우리가 계속 끌고 갈 변경일 때. 업스트림 추적 비용이 생기므로 마지막 수단.

### ⑤ 벤더링된 저장소 직접 수정

`<엔진>/repo/` 는 이미 우리 것이므로 그냥 고치면 된다.
다만 업스트림 갱신 시 병합 부담이 생기니, **변경을 한 곳에 모으고 주석으로 표시**할 것.

---

## 3. 학습으로 풀 것과 아닌 것

지금까지 관측된 문제를 원인별로 나눈 결과다. **학습이 답인 경우는 생각보다 적다.**

### 학습이 정답인 것

| 대상 | 문제 | 필요 데이터 | 비고 |
|---|---|---|---|
| zipformer | "합성"→"학생" 오인식 | 도메인 한국어 음성 **수십 시간**(전사 포함) | 모델이 작아 학습 비용이 낮다. icefall/k2 에 파인튜닝 레시피가 있다 |
| moss-tts-nano | 한국어 프리셋 음색 부재 | 한국어 화자 **수 분~수십 분** | 저장소에 `finetuning/` 이 있다. TTS 화자 적응은 ASR 보다 훨씬 가볍다 |

### 학습이 아니라 다른 층에서 풀리는 것

| 대상 | 문제 | 실제 해법 |
|---|---|---|
| zipformer | 띄어쓰기 없음 | **해결됨** — server.py 에서 토큰 이어붙이기 (위 ① 참고) |
| moss-tts-nano | 한국어 품질 저하 | **해결됨** — 참조 음성 복제. 내장 프리셋에 한국어 음색이 없던 것이 원인 |
| cosyvoice | 숫자·기호 읽기 | wetext 확보 문제. ModelScope 403 으로 다운로드가 안 돼 텍스트 정규화가 꺼져 있다 (`no frontend is avaliable`) |

### 학습으로 안 되는 것

| 대상 | 문제 | 이유 |
|---|---|---|
| qwen3-tts | RTF 6.5 (느림) | 파인튜닝은 품질을 바꾸지 실행 속도를 바꾸지 않는다. 12Hz 프레임마다 다중 코드북을 순차 생성하는 구조가 원인 |
| xtts | torchcodec 로드 실패 | 환경 문제 |

---

## 4. 이 장비에서 학습이 가능한가

가능하지만 조건이 있다.

**VRAM 16GB (RX 7800 XT / gfx1101)**

| 대상 | 가능 여부 |
|---|---|
| zipformer 파인튜닝 | 여유 있음 (소형 transducer) |
| TTS 화자 적응 (moss/cosyvoice) | 여유 있음 |
| Whisper large-v3 전체 파인튜닝 | 어려움 → LoRA 로 우회 |

**ROCm on WSL 은 추론보다 학습이 불안정하다.** 학습은 장시간 실행이라 아래 문제에 훨씬 많이 노출된다.

- MIOpen 커널 캐시가 깨지면 conv 가 `miopenStatusUnknownError` 로 실패한다
  (실제로 겪었다. `/models/.miopen` 을 지우면 복구된다 — compose 에 경로를 박아뒀다)
- ROCm 프로파일러·디버거는 **지원되지 않는다**. 학습 병목 분석 도구가 없다는 뜻이다
- `ROCPROFILER_REGISTER_ENABLED=0` 이 없으면 `torch.cuda.device_count()` 에서 프로세스가 abort 한다

학습을 본격화한다면 **네이티브 리눅스 부팅을 검토할 가치가 있다.** WSL 의 ROCDXG 경로는
추론 성능 손실은 없었지만(컨테이너 fp16 50.1 TFLOPS), 학습 도구 체인은 제약이 크다.

---

## 5. 권하는 순서

1. ~~zipformer 띄어쓰기~~ **완료** (server.py 한 줄)
2. **wetext 확보** — cosyvoice 숫자·기호 읽기 정상화
3. **실제 사용할 음성으로 현재 정확도 측정** — 지금 수치는 짧은 합성 문장 기준이다.
   실데이터에서 Whisper 만으로 충분할 수도, 반대로 파인튜닝이 꼭 필요할 수도 있다
4. 3번 결과에 따라 **zipformer 도메인 파인튜닝**

측정 없이 학습부터 시작하지 말 것. 오늘 zipformer 띄어쓰기가 그 예다 —
학습 문제로 보였지만 실제로는 우리 코드 한 줄이었다.
